void remove(celula *p, celula *ini){
    celular = *morta;
    morta = p->prox;

    if morta->prox == NULL{
        p->prox = NULL;
    } else{
        p->prox = morta->prox;
        free(morta);
    }
}